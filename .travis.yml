language: java
jdk:
  - oraclejdk11
before_install:
  - chmod +x gradlew
  - chmod +x gradle/wrapper/gradle-wrapper.jar
script:
  - ./gradlew clean build -b consumer/build.gradle.kts
  - ./gradlew clean build -b provider-spring/build.gradle.kts
